/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'sse.so',
            'localhost',
            'res.cloudinary.com',
            'sse-backend-v6ub.onrender.com'
        ]
    }
};

export default nextConfig;
