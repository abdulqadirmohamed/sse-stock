/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'sse.so',
            'localhost',
            'res.cloudinary.com'
        ]
    }
};

export default nextConfig;
